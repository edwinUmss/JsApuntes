/**
 * array.forEach((item) => {
 *  codigo a ejecutar
 * })
 */

let list = ["eat", "sleep", "repeat", "code"];

list.forEach((item) => {
    console.log(item)
})

//realiza un recorrido de todos los elementos del array y ejecuta el código que se encuentra dentro de la función que se le pasa como argumento. En este caso, se imprime cada uno de los elementos del array.